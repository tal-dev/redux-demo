import { LOGIN } from "../actions/actions.types"

const initialState = {
    user: null
}

const login = (state = initialState, action) => {
    switch (action.type) {
        case LOGIN:
            return {

            }
    }
}

export default login